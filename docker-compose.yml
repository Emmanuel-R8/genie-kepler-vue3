version: "3.5"
services:
  nginx:
    container_name: nginx
    image: nginx:stable-alpine
    restart: on-failure
    depends_on:
      - golang
    ports:
      - "80:80"
    networks:
      - app-network
    volumes:
      - ./logs:/var/log/nginx
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./public-build:/var/www/public:ro

  golang:
    container_name: golang
    # build: .
    image: geospatialweb/go-vue3-tsx:latest
    command: /app/main
    restart: on-failure
    depends_on:
      - postgres
    ports:
      - "8000:8000"
    networks:
      - app-network

  postgres:
    container_name: postgis
    image: kartoza/postgis:12.0
    restart: on-failure
    ports:
      - "5432:5432"
    networks:
      - app-network
    volumes:
      - ./postgres/postgis.sql:/docker-entrypoint-initdb.d/postgis.sql
    environment:
      POSTGRES_DBNAME:  ${POSTGRES_DBNAME}
      POSTGRES_PASS:    ${POSTGRES_PASSWORD}
      POSTGRES_USER:    ${POSTGRES_USER}

networks:
  app-network:
    driver: bridge
